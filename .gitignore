# Runtime state stays local.
state/
.mcg-state/
tasks/
logs/
events/inbox/
*.pid
*.log

# Local Wire credentials stay outside Git.
config/wire.json

# Editor and recovery backups stay outside Git.
*.bak-*
