import type { ApprovalRequest } from './approval-gate';import type { ApprovalPersistence } from './approval-store';import type { SqlExecutor } from './postgres-persistence';
export class PostgresApprovalStore implements ApprovalPersistence {constructor(private readonly db:SqlExecutor){}async save(a:ApprovalRequest):Promise<void>{await this.db.query('insert into maestri_approvals(approval_id,task_id,risk,status,payload) values($1,$2,$3,$4,$5::jsonb) on conflict(approval_id) do update set status=excluded.status,payload=excluded.payload',[a.approval_id,a.task_id,a.risk,a.status,JSON.stringify(a)])}async forTask(taskId:string):Promise<ApprovalRequest|undefined>{const r=await this.db.query<{payload:ApprovalRequest}>('select payload from maestri_approvals where task_id=$1 order by created_at desc limit 1',[taskId]);return r.rows[0]?.payload}}
