export class IdempotencyStore<T>{private readonly records=new Map<string,T>();get(key:string):T|undefined{return this.records.get(key)}set(key:string,value:T):void{if(this.records.has(key))throw new Error(`idempotency_key_already_exists:${key}`);this.records.set(key,value)}async once(key:string,operation:()=>Promise<T>):Promise<T>{const existing=this.get(key);if(existing!==undefined)return existing;const value=await operation();this.set(key,value);return value}}
export function executionIdempotencyKey(planId:string,taskId:string,baseSha:string,attempt:number):string{return[planId,taskId,baseSha,attempt].join(':')}
