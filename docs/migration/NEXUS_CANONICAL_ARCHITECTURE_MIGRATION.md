# Nexus Brain — mapa de migração da arquitetura canônica

**Estado autoritativo em 2026-09-26:** estrutura local organizada e validada; dois MCPs antigos foram encerrados; launcher de autostart foi arquivado, Scheduled Task removida e MCG_ROOT obsoleto limpo. Os jobs de CI terminaram; PR/merge e renomeação física do checkout ainda pendem. O check agregado CodeQL permanece vermelho por findings em paths legados movidos, sem supressão.
**Origem Git:** `trydavidqix/nexus-brain`, branch `main` em `5d78258893cf25ce4c6038999a82e1e2be97e8a7`
**Checkout desta execução:** worktree isolado `codex/nexus-canonical-architecture`.

> **Separação de escopo:** esta atividade é somente `MIGRATION READINESS`. Não iniciar APIs, cloud services, funcionalidades, integrações ou work packages futuros do Master Blueprint. O Blueprint continua sendo o único tracker da próxima fase.

## Snapshot autoritativo da migração — 2026-09-26

- O workspace agora contém 10 packages (`brain`, `compat`, `context-gateway`, `contracts`, `control-plane`, `evidence`, `execution`, `governance`, `providers`, `routing`) e 3 apps (`cli`, `control-center`, `edge`). Não foram criados `brain-api`, `worker`, SDK, código de Everything Journal, infraestrutura cloud falsa ou pastas vazias.
- Código existente, testes, contratos, registries, configs, ferramentas, hooks e documentação foram distribuídos aos owners atuais. O SQL schema existente está em `infra/cloud/database`; arquivos de tipos acompanham os apps/packages; hooks existentes estão em `tooling/hooks`; dados de config foram separados em `config/{budgets,environments,policies,registries}`.
- `packages/compat` e os shims de raiz preservam contratos legados de forma explícita; não são owners alternativos. MCG/Token Firewall continua no Nexus. Wire é opcional e o Nexus não requer processo/app Maestri.
- Validação local repetida: frozen install passou; 163 testes unitários, 15 testes de integração, 13 typechecks, arquitetura com 13 packages e sem ciclos/imports não declarados, 113 módulos no syntax/import smoke, scan sensível em 224 arquivos, fuzz de redaction com 45.993 execuções/11s, hooks observacionais exercitados e `git diff --check` passaram. `eval:smoke` não é gate local offline: foi interrompido quando se constatou que fazia chamadas reais ao Codex; uma lane de baseline foi escrita, sem concluir o par.
- Auditoria Windows final parcial: nenhum daemon MCG/Maestri/Wire ou processo MCP legado permanece. A tarefa agendada foi exportada e removida; shortcut/VBS foram arquivados; launchers globais `.local/bin` agora apontam ao CLI no novo caminho planejado e usam stop gracioso; MCG_ROOT de usuário foi removido. O `.mcg-state` efetivamente localizado continha somente os dois arquivos da lane interrompida (18.571 bytes); ambos foram copiados para `.nexus-state` e comparados por caminho relativo e SHA-256; originais retidos. A contagem histórica de 417 arquivos não se reproduz e permanece sem fonte comprovada.
- O diretório antigo continua sendo o `cwd` registrado da tarefa Codex atual, embora a execução de código esteja isolada em outra worktree. Não renomear até confirmar que o app continuará abrindo a tarefa após o `git worktree move`; o caminho local físico ainda está pendente.
- O `doctor` do CLI não consulta mais `MAESTRI_CLI` nem executa `maestri debug`; isso removia uma invocação externa desnecessária e deixa o diagnóstico autônomo do Maestri. Teste dedicado cobre essa garantia.
- No PR #44, MCG gates, instalação congelada/workspace, testes, CodeQL extraction, Gitleaks, Semgrep, OSV, ZAP e fuzz executaram; CodeQL anota findings em arquivos migrados e mantém o check agregado vermelho. A `main` já contém alertas críticos correspondentes em código-fonte antigo (incluindo execução de comandos e Wire SSRF). Não foram suprimidos nem declarados corrigidos por esta reorganização.
- O restante deste documento conserva snapshots e planos anteriores; quando contradisser este snapshot, este bloco é a verdade operacional mais recente.

O caminho global do Codex foi atualizado para confiar no futuro checkout canônico. Os dois launcher trees MCP antigos foram parados; a tarefa agendada removida após exportar seu XML; shortcut e VBS arquivados. Não há processo Node MCG/Wire/daemon ativo observado. O state root previamente relatado com 417 arquivos segue ausente, não foi apagado nesta tarefa e não pode ser declarado migrado.

Este mapa complementa o tracker único `docs/blueprints/NEXUS_BRAIN_MASTER_IMPLEMENTATION_BLUEPRINT.md`; não é um segundo tracker. É proibido mover um grupo antes de cumprir sua linha de pré-condições e atualizar testes/imports no mesmo changeset.

## 1. Inventário AS-IS confirmado

No snapshot inicial da branch base havia aproximadamente 300 arquivos versionados em `src/`, `packages/operating-core/`, `packages/local-runtime/`, `test/`, `contracts/`, `registry/`, `config/`, `bin/`, `scripts/`, `hooks/`, `types/`, `evals/`, `docs/` e `.github/`; ainda não existiam `apps/`, `tooling/`, `tests/`, `docs/architecture/` ou `docs/adr/`. O workspace PNPM inicial tinha dois packages internos. Após as ondas locais atuais, há sete projetos de workspace: `packages/contracts`, `brain`, `control-plane`, `evidence`, `context-gateway`, `operating-core` e `apps/edge`; nenhuma pasta `everything/` vazia foi criada porque não há implementação de Everything para mover ainda.

O grafo inicial detectou zero imports relativos quebrados depois de tratar corretamente extensões `.js` usadas por TypeScript. Há 10 imports de `bin/` para `src/` e 43 de `test/` para `src/`; esses consumidores precisam migrar junto com os módulos. Os pacotes operating-core e local-runtime têm imports internos próprios. O diretório `cloud-fabric` é altamente acoplado e sua divisão exige portas/contratos explícitos antes de separar pacotes.

O diretório local ignorado `state/` contém estado de execução fora do Git; não será tratado como código-fonte nem movido sem plano de retenção/restauração. A raiz contém apenas manifests, README, LICENSE e `.gitignore`, que continuam na raiz do monorepo. Arquivos históricos sob `docs/archive/` e `docs/archive/source-blueprints/` permanecem como fontes de proveniência, não como código ativo.

## 2. Mapa AS-IS → TARGET e decisões de ownership

| Origem atual | Destino canônico | Regra de migração / estado | Pré-condição de aceite |
|---|---|---|---|
| `src/context/compiler.mjs` | `packages/brain/src/compiler.mjs` | Migrado; mantém-se `src/context/compiler.mjs` como reexport compatível. | Teste do pacote Brain, suíte MCG, typecheck e gate arquitetural passam. |
| `src/context/cache.mjs` | `packages/evidence/src/context/cache.mjs` | Migrado com history store; cache registra métricas observadas/estimadas e depende somente do histórico Evidence. Caminho antigo é reexport. | Teste Evidence e teste de compatibilidade MCG passam. |
| `src/context/memory.mjs` | `packages/brain/src/memory.mjs` | Migrado após comparar implementações: este é o único store persistente atual; `operating-core/memory` contém contratos/interfaces e `MemoryRetriever` do Cloud Fabric usa registros fornecidos em memória. Dados JSONL não foram movidos. | Testes Brain e MCG passam; reexport antigo preserva consumidores; ownership registrado em ADR. |
| `src/history/store.mjs` | `packages/evidence/src/history/store.mjs` | Migrado como ledger append-only/projeção de observabilidade compartilhada; consumidores antigos usam reexport compatível. Estado local não foi movido. | Testes de history/dashboard e integridade do armazenamento passam. |
| `src/core.mjs`, `src/contracts.mjs`, `src/registry.mjs` | `packages/context-gateway/src` e `packages/contracts/src` | `core.mjs` é o adaptador MCG local com task store em disco; schemas/validação migram para contracts. Não substituir silenciosamente o task store pelo `operating-core/job-engine.ts`, que é outro protótipo em memória. | Contrato de gateway versionado, testes de compatibilidade e nenhuma duplicação de task store aceita sem ADR. |
| `src/events/bus.mjs`, `evidence.mjs`, `replay.mjs`, `telemetry.mjs`, `traces.mjs`, `scores.mjs` | `packages/evidence/src/events`, `replay`, `telemetry`, `traces`, `provenance` | Eventos e evidência observacional; separar telemetria de domínio se os contratos mostrarem donos diferentes. | Schemas e consumidores mapeados; redaction tests preservados. |
| `src/redaction.mjs` e `packages/operating-core/src/cloud-fabric/redaction.ts` | `packages/evidence/src/redaction` | Há funções sobrepostas, mas contratos/escopos diferem. Criar API comum com adaptadores compatíveis; não apagar uma implementação até provar equivalência por testes de segredos, estruturas recursivas e preservação de métricas. | Testes de contrato cobrindo ambos os formatos e todos os consumidores. |
| `src/executor.mjs`, `worktrees.mjs` | `packages/execution/src/executor`, `worktrees` | Bibliotecas reutilizáveis de execução isolada. | Consumidores e política de segurança verificados. |
| `src/daemon-control.mjs`, `src/provider-discovery.mjs`, `src/wire.mjs`, MCG daemon em `bin/mcg.mjs`, Local Runtime | `apps/edge/src/{daemon,bridge,everything,git}` e `packages/context-gateway` | Local Runtime vive em `apps/edge`; Wire client e daemon-control migraram para `apps/edge/src/{bridge,daemon}`; ambos mantêm reexports legados para CLI/dashboard. Wire consome `ROOT` via `@nexus-brain/context-gateway/core`. `provider-discovery.mjs` pertence ao pacote Context Gateway. `apps/edge/src/everything` fica adiado até existir implementação do Everything Journal. O daemon MCG observado é um Wire/inbox bridge ativo; seu processo/configuração/dados continuam intactos até cutover próprio, sem confundi-lo com o Local Runtime MCP. | Testes Edge (incluindo stop autenticado), suíte MCG e typecheck passam. Ainda falta migrar CLI/dashboard e fazer cutover operacional de MCG_ROOT + autostart após redirecionar consumers MCP; verificar Wire e impedir instância dupla. |
| `src/dashboard.mjs`, `dashboard-ui.mjs` | `apps/control-center/src` | Dashboard MCG de operação local. | Rotas, assets, título, acessibilidade e contratos de dados passam testes no novo caminho. |
| `packages/operating-core/src/context/**`, `memory/**`, `knowledge/**`, `learning/**` | `packages/brain/src/{context,memory,knowledge,learning}` | Preservar lógica temporal/proveniência e consolidar com `src/context` somente após comparar APIs; não criar segundo memory/context engine. | Escolha de implementação canônica registrada em ADR; consumidores e testes migrados. |
| `packages/operating-core/src/session/session-engine.ts`, `job-engine.ts`, `job-claim-store.ts`, `event-log-adapter.ts`, `receipt-store.ts` | `packages/control-plane/src/{sessions,tasks,events}` | Migrados; Operating Core conserva reexports compatíveis e declara dependência workspace para Control Plane. Estes stores/protótipos do Maestri continuam separados do task store persistente MCG até integração por contrato. | Testes Control Plane/Operating Core e typechecks passam; grafo acíclico. |
| `packages/operating-core/src/workforce/workforce-scheduler.ts` | `packages/control-plane/src/workforce/workforce-scheduler.ts` | Migrado com teste de escopo de turnos; Operating Core mantém reexport. É scheduler de turnos in-memory, não o executor/workforce orchestrator cloud. | Teste de turno, typecheck e arquitetura passam. |
| Demais `packages/operating-core/src/workforce/**`, `executive/**` | `packages/control-plane/src/{workforce,approvals}` | Pendentes; classificar council e workforce cloud antes de mover. `executive` só migra se tiver chamadas operacionais; evitar outro scheduler autoritativo. | Contratos de task/session/event definidos; não criar duas filas/schedulers autoritativos. |
| `packages/operating-core/src/router/**` e `cloud-fabric/*router*` | `packages/routing/src` | Distinguir model/provider/resource/quota routing. `router/quota-router.ts` é stub de quota de tenant e difere do quota-router provider-aware; confirmar exports/consumidores antes de remover ou consolidar. | Matriz de chamadas e testes por tipo de roteador; dependências cíclicas removidas via ports/contracts. |
| `packages/operating-core/src/cloud-fabric/{claude,codex,antigravity}-adapter.ts`, `provider-health.ts`, `provider-selection.ts`, `capability-registry.ts` | `packages/providers/src/{claude,codex,antigravity}` | Adaptadores e seleção/health vão para providers; protocolos/ports neutros vão para contracts. `jules` e `gemini` só recebem diretório quando houver código real. | Contratos provider-neutral e testes de adapter mantidos. |
| Demais `cloud-fabric` — execução, retry/recovery, approvals, budgets, policy, evidence, telemetria, persistence e dashboard snapshots | Decompor entre `packages/{execution,control-plane,governance,evidence,contracts,routing,brain,providers}` e `apps/control-center` | `execution-loop`, `retry-controller`, `run-recovery`, `worktree-manager` → execution; `approval-*`, `budget-policy`, `cost-ledger`, `master-plan`, `workforce-orchestrator` → control-plane; `*policy`, `scope-guard`, `acceptance`, `contract-validation` → governance; `evidence-*`, `telemetry-*`, `trace-*`, `result-digest`, `redaction` → evidence; `execution-port`, `context-packet`, `workforce-types` → contracts; `gateway-adapter` → provider adapter `gateway`. `dashboard-service/snapshot` são read models/API de dados, não um segundo servidor/dashboard. `postgres-schema.sql` → `infra/cloud/database`; `durable-stores.ts` e `postgres-persistence.ts` precisam ser divididos por dono (brain, control-plane, evidence) em vez de criar um pacote de persistência compartilhado que gere ciclos. | Arquivo por arquivo e grafo de dependências sem ciclos; nenhum ciclo mascarado por barrels. Cada módulo classificado ganha teste no pacote consumidor. |
| `contracts/*.v1.schema.json`, `src/contracts.mjs`, `packages/operating-core/src/contracts.ts`, `packages/local-runtime/src/contracts.ts` (já migrados) | `packages/contracts/{schemas,src}` | ✅ Validador JS, 12 schemas e tipos TS de Job e Runtime têm fonte única em `packages/contracts`; os caminhos antigos são re-exports compatíveis. Tipos ficam nos submódulos `job` e `runtime`, sem mesclar contratos incompatíveis. | ✅ testes JS/TS, package typecheck e typecheck dos dois consumers passam. |
| `registry/*.json` (já migrado para `config/registries/*.json`) | `config/registries/*.json` | Os seis registries são dados de configuração. As fontes internas agora apontam para arquivos reais; origens que pertencem a outro runtime usam `external://` e permanecem `UNAVAILABLE`, sem fingir que são arquivos locais. | ✅ teste `registry.test.mjs` valida existência e confinamento de cada fonte local. |
| `config/*.json` | `config/policies`, `config/budgets`, `config/environments` | `usage-budgets.json` → budgets; `anomaly-rules.json`/`efficiency-score.json` → policies; `default.json` e `wire.example.json` → environments/templates conforme consumo. | CLI, daemon e dashboard leem os mesmos arquivos após atualização. |
| `bin/mcg.mjs`, `mcg-eval.mjs`, `parse-codex-usage.mjs`, `mcg-run.ps1` | `apps/cli/src` e/ou `apps/edge/src` | Entradas CLI de usuário → `apps/cli`; inicialização PowerShell/daemon do Edge → `apps/edge` ou `tooling/scripts`, conforme função. Não transformar executável em package reutilizável. | CLI smoke tests e launcher Windows passam; MCP/daemon usam entrypoints canônicos. |
| `hooks/*`, `scripts/*` | `tooling/hooks`, `tooling/scripts` | Hooks Git ficam em hooks; verificação, sensibilidade, setup/autostart e helpers vão para scripts, com ownership/caller documentado. | Referências nos workflows, package scripts, Git config e tarefas Windows atualizadas. |
| `types/*` | `packages/{context-gateway,control-center,evidence}/src` ou `packages/contracts/src` | Tipos acompanham o domínio que os publica; evitar diretório global de tipos sem dono. | Declarações de export apontam para build/source válido. |
| `test/*.test.mjs` | Testes unitários adjacentes aos pacotes; cross-system em `tests/integration`/`tests/e2e`; `test/fuzz/**` → `tests/fuzz`; fixtures compartilhados → `tests/fixtures` | Classificar cada um dos 30 testes pelo módulo exercitado; não mover teste de integração para junto do módulo por conveniência. | Inventário teste→módulo, comandos CI atualizados, cobertura equivalente ou maior. |
| `packages/operating-core/src/**/{*.test.ts,**/__tests__/**}` e Local Runtime tests (já movidos) | Diretório `tests/` junto ao package/app dono | Edge tests agora ficam em `apps/edge/tests`; Operating Core ainda requer migração junto de cada domínio. Separar apenas integração cross-package. | Vitest/typecheck no destino e launcher MCP de ponta a ponta. |
| `evals/**` e `src/evals.mjs`, `src/eval-runner.mjs` | `evals/{datasets,graders,benchmarks,reports}` e runner em `apps/cli` ou `packages/evidence` | Dados ficam em evals; lógica de avaliação fica no CLI ou evidence conforme API/consumidores. | Smoke/validation atuais funcionam com paths novos e relatórios versionados sem secrets. |
| `docs/ARCHITECTURE.md`, `docs/IMPLEMENTATION_PLAN.md`, `docs/MAESTRI_CONTEXT_GATEWAY_MASTER_PLAN.md`, `docs/plans/maestri-codex-cloud-permanent-integration.md`, `docs/superpowers/plans/2026-09-23-dashboard-reports-token-firewall.md`, `docs/STATUS.md`, `docs/security*`, `docs/maestri/**`, arquivos de histórico/fontes | `docs/architecture`, `docs/blueprints`, `docs/blueprints/sources`, `docs/blueprints/archive`, `docs/migration`, `docs/runbooks`, `docs/security`, `docs/archive` | Master Blueprint permanece tracker único. `docs/IMPLEMENTATION_PLAN.md` e `docs/MAESTRI_CONTEXT_GATEWAY_MASTER_PLAN.md` serão mantidos como material de compatibilidade/histórico com banner e link ao tracker, não apagados. Fontes históricas preservam nome original e proveniência; não renomear texto histórico por busca cega. | Links verificados, fontes intactas, decisões novas em ADR. |
| `.github/workflows/*`, `dependabot.yml` | `.github/workflows/*` e atualização de ownership/paths | Workflows recebem paths/cobertura novos; CodeQL/security/fuzz executam de modo que checks required nunca fiquem pending por path filter. Atualizar Dependabot para PNPM workspace. | O gate `pnpm check:architecture` já roda no CI; validação remota do PR ainda pendente. |
| `.github/` sem CODEOWNERS/templates | `.github/CODEOWNERS`, `pull_request_template.md`, `ISSUE_TEMPLATE/*` | Criar apenas com owners reais confirmados. Não inventar pessoa/time nem tornar regras de proteção fictícias. | `CODEOWNERS` sintaticamente válido e owners com acesso; templates coerentes com evidência/blueprint. |
| `README.md`, root `package.json`, `pnpm-workspace.yaml`, `pnpm-lock.yaml`, `tsconfig.json` | Raiz monorepo | Raiz fica como orquestrador. Workspaces cobrem apenas packages/apps existentes; lockfile gerado por PNPM, não editado manualmente. Edge package agora é `@nexus-brain/edge`; preservar o protocolo/tool MCP legado como contrato de compatibilidade. | `pnpm install --frozen-lockfile`, scripts, exports, CLI e CI verdes. |
| Sem origem hoje: `apps/brain-api`, `apps/worker`, `packages/sdk`, `infra/cloud/*` e `infra/windows/*` | Criar somente junto com implementação executável/IaC/serviço real | Não criar diretórios vazios para espelhar a árvore. Infra atual ainda é decisão/blueprint, sem IaC que possa ser movida. | Primeiro módulo real, testes e owner definidos. |
| Checkout local `%USERPROFILE%\.lumenva\maestri-context-gateway` | `%USERPROFILE%\.lumenva\nexus-brain` | Renomear somente após liberar processos/sessões/handle, atualizar consumidores e evitar conflito com o checkout de trabalho atual. O diretório é histórico de clone, não código a migrar. | NB-01 unblock protocol, `git worktree list`, Git remote/branch, hooks, MCP, daemon, launcher e search global pós-move aprovados. |

### Arquivos atuais classificados por destino

**`src/` (27 arquivos no inventário inicial):** compiler/memory → `packages/brain`; cache/history/events/evidence/telemetry/traces/scores/redaction → `packages/evidence`; contracts → `packages/contracts`; core/registry/provider-discovery → `packages/context-gateway`; daemon-control e Wire → `apps/edge` (migrados com reexports compatíveis); executor/worktrees → execution; dashboard/UI → control-center; routing/router → routing; scheduler/DAG → control-plane; codex usage → adapter Codex ou normalização Evidence após call-graph; evals/runner → eval boundary. Cada export original permanece ou recebe adapter compatível testado.

**`bin/` (4 files):** `mcg.mjs`, `mcg-eval.mjs`, `parse-codex-usage.mjs` → CLI entrypoints; `mcg-run.ps1` → Windows launcher under `apps/edge` or `tooling/scripts` based on its caller. The legacy command `mcg` stays as an alias until usage/config migration is validated.

**`test/` (30 files):** `anomalies`, `codex-usage`, `context`, `contracts`, `core`, `dashboard-accessibility`, `dashboard`, `eval-runner`, `evals`, `evidence`, `executor`, `history-store`, `memory-os`, `metrics`, `provider-discovery`, `redaction`, `registry`, `replay`, `router-dag`, `runtime-root`, `scheduler`, `scores`, `stats`, `telemetry`, `traces`, `worktrees` → unit tests beside the owning package/app; `daemon.integration`, `mcp-cli`, `mcp-probes` → `tests/integration` or `tests/e2e` after classifying actual scope; `fuzz/redaction.fuzz.mjs` → `tests/fuzz`; `core` fixture/event helpers remain with their tests unless imported by more than one domain, in which case `tests/fixtures`. `wire-security.test.mjs` is a preexisting uncommitted MCG security regression and remains separately owned by that MCG change, not this architecture move.

**`packages/operating-core/src/` top-level:** `autonomy/**`, `psyche/**`, `outcome-os/**`, `executive/**`, and `evals/**` are not represented as explicit target domains. They must be mapped by call graph before moving: autonomy policy/execution decisions → execution or governance; outcome verifier/economics → evidence/control-plane; executive council/decision ledger → control-plane; eval harness → `evals/` or CLI. If a directory is an unused experiment with no in-scope consumer, retain it in place until a separate preserve/archive decision; do not silently delete. `backup/dr-engine.ts` → recovery boundary (execution/recovery or `infra/recovery` after distinguishing runtime from storage); retain current public API through adapter until callers are migrated.

**Active MCG daemon finding:** `bin/mcg.mjs daemon` is not the Local Runtime MCP server. It watches the MCG task inbox and, with Wire config present, subscribes to Maestri Wire’s event feed. The live Windows daemon connects to the Maestri app on loopback port 7434. Its `MCG_ROOT` currently points to a separate Desktop clone with hundreds of state/task files; no state has been moved. The duplicate Scheduled Task that launched the Desktop clone has been disabled, while the Startup shortcut and active daemon are preserved. Before NB-29, hash/backup and migrate that state, update the user `MCG_ROOT`, and replace the shortcut with one canonical Edge daemon startup. The full evidence and field-by-field ownership report is in `docs/blueprints/NB-01_SOURCE_OWNERSHIP_AND_LOCAL_PATH_AUDIT.md`.

**`packages/operating-core/src/cloud-fabric/` exact source classification:**

- Providers: `antigravity-adapter.ts`, `claude-adapter.ts`, `codex-adapter.ts`, `gateway-adapter.ts`, `provider-health.ts`, `provider-selection.ts`, `capability-registry.ts`.
- Routing: `learning-resource-router.ts`, `learning-router.ts`, `model-registry.ts`, `quota-broker.ts`, `quota-router.ts`, `resource-router.ts`, `routing-observability.ts`, `routing-policy.ts`.
- Brain/context read support: `context-cache.ts`, `context-resolver.ts`, `history-persistence.ts`, `history-query.ts`, `memory-retriever.ts`.
- Control-plane: `approval-gate.ts`, `approval-store.ts`, `persistent-approval-store.ts`, `budget-policy.ts`, `cost-ledger.ts`, `department-profiles.ts`, `master-plan.ts`, `plan-status.ts`, `workforce-orchestrator.ts`.
- Execution/recovery: `cloud-job.ts`, `delegation.ts`, `escalating-executor.ts`, `escalation-engine.ts`, `escalation-record.ts`, `execution-loop.ts`, `handoff.ts`, `idempotency.ts`, `persistent-run-store.ts`, `resume-controller.ts`, `retry-controller.ts`, `reviewer-executor.ts`, `run-recovery.ts`, `run-state.ts`, `worktree-manager.ts`, `worktree-policy.ts`.
- Shared contracts/ports: `execution-port.ts`, `context-packet.ts`, `workforce-types.ts` → `packages/contracts/src` or domain schemas where their runtime validation belongs. This is a dependency-cycle break, not a new domain implementation.
- Evidence/validation: `acceptance.ts` (governance/evidence boundary to decide), `benchmark-harness.ts`, `evidence-bundle.ts`, `evidence-gate.ts`, `independent-review.ts`, `persistent-telemetry.ts`, `redaction.ts`, `result-digest.ts`, `telemetry-event.ts`, `trace-factory.ts`, `validation-corpus.ts`, `validation-report.ts`.
- Governance: `autonomous-policy.ts`, `contract-validation.ts`, `contract-version.ts`, `handoff-guard.ts`, `runtime-policy.ts`, `scope-guard.ts`.
- Infra/persistence: `durable-stores.ts`, `postgres-persistence.ts`, `postgres-schema.sql`, `port-registry.ts`, `tool-registry.ts` (registry ownership must follow whether it is policy or provider metadata), plus the persistence portions of the approval/run/history/telemetry stores. Keep domain ports in their owning packages; only concrete infrastructure/schema belongs in `infra`.
- Control Center read model: `dashboard-service.ts`, `dashboard-snapshot.ts`; they are data query/report modules, not an additional dashboard server.
- Existing tests: `__tests__/tokens-fabric.test.ts`, `antigravity-adapter.test.ts`, `claude-adapter.test.ts`, `cost-ledger.test.ts`, `delegation.test.ts`, `execution-port.test.ts`, `handoff.test.ts`, `model-registry.test.ts`, `resource-router.test.ts`, `result-digest.test.ts`, `scope-guard.test.ts`, `workforce-orchestrator.test.ts` migrate with the owned module; shared workflow tests become integration tests only if they span packages.
- `index.ts` is a compatibility export barrel. It may be reduced only after export/import usage is enumerated and compatibility tests prove consumers no longer require legacy paths.

**Static contracts/config/docs:** 12 schemas versionados migraram por domínio para `packages/contracts/schemas/<domain>/`; validador JS em `packages/contracts/src/index.mjs`; tipos TS de Job e Runtime em `packages/contracts/src/{job,runtime}.ts`. Os antigos caminhos de contrato são re-exports sem lógica duplicada. Os seis registries migraram um a um de `registry/` para `config/registries/`. `config/{anomaly-rules,default,efficiency-score,usage-budgets,wire.example}.json` map by consumer as described above. `types/{context/compiler,dashboard,telemetry,traces}.d.ts` follow their exported domain. Existing `.agents/roles/*` and `.agents/skills/jules-delegation/SKILL.md` remain project-scoped under `.agents`; `.codex/config.toml` remains project-scoped and its launcher now targets the migrated Edge app. The three workflows plus Dependabot config stay in `.github` with path and workspace corrections. `evals/datasets/{pilot,smoke,validation,validation-v2}.jsonl` stay in `evals/datasets`; they are inputs, not executable packages.

## 3. Duplicações e fronteiras que exigem decisão/teste

1. **Redaction:** existem implementações parecidas, não comprovadamente equivalentes. Consolidar API, preservar wrappers antigos e provar por fixtures/testes de contrato antes de retirar uma.
2. **Tasks/scheduler:** `src/core.mjs`/`src/scheduler/*` são persistentes e locais; `operating-core/job-engine.ts` é in-memory; `workforce-scheduler.ts` é shift/wakeup; `cloud-fabric/workforce-orchestrator.ts` é execução provider-aware com evidência. Não são intercambiáveis. Definir modelo canônico, stores e adapters para não manter quatro autoridades.
3. **Routing:** roteador local de tool-risk, routers de modelo/IA, quotas de tenant e provider/resource/quota têm políticas diferentes. Uma pasta destino comum não autoriza uni-los em uma função.
4. **Dashboard:** servidor/UI MCG e dashboard snapshots de workforce são capacidades separadas. Um app control-center pode consumi-las, mas não se deve manter dois servidores ou duplicar APIs de relatório.
5. **Context/memory:** compilador/cache do MCG e context engine/memory kernel do Operating Core têm níveis e contratos distintos. Compilador e store persistente MCG foram extraídos para `packages/brain`; `memory-kernel.ts` permanece como contrato de armazenamento e `MemoryRetriever` como seletor sobre registros fornecidos, sem segunda persistência. O cache continua no local antigo até sua fronteira com histórico/evidence ser decidida.
6. **Registry source paths:** referências em `registry/*.json` apontam a caminhos inexistentes sob `packages/maestri-context-gateway`. Isso já afeta a exatidão do relatório; corrigir com mapeamento de source real, sem falsificar estado de disponibilidade.

## 4. Regras de dependência-alvo

Direção permitida: `apps → packages`; `control-plane → contracts, brain, execution, routing, providers, evidence, governance`; `execution → contracts, providers, evidence, governance`; `routing → contracts, providers`; `providers → contracts`; `brain → contracts`; `evidence → contracts`; `governance → contracts`; `context-gateway → brain, contracts, evidence`; `edge → contracts, context-gateway/SDK HTTP-MCP ports`, sem importar stores autoritativos do cloud control-plane. Telemetria de routing entra em evidence como DTO/port de contracts, nunca por import direto de implementação.

**Snapshot inicial pré-migração (histórico):** o primeiro import scan apontou ciclos planejados entre módulos que então estavam no mesmo pacote. A migração posterior extraiu os owners e `pnpm check:architecture` agora passa em 13 packages, sem ciclos ou imports internos não declarados. O texto inicial fica como histórico da motivação, não como estado atual.

## 5. Ondas seguras e gates

| Onda | Escopo | Gate antes de avançar |
|---|---|---|
| A — contrato/base | Fechar owners por arquivo, export/API map, schemas, boundary DAG e testes de compatibilidade; corrigir registries inválidos com testes. | Grafo sem ciclos planejados; teste adversarial da arquitetura falha para import proibido; manifests do workspace documentados. |
| B — pacotes de baixa dependência | Migrar contratos, evidence/redaction e context/compiler em pequenos grupos; corrigir imports/testes no mesmo patch. | Teste alvo falhou antes, passa depois; nenhum consumidor antigo quebrado; pnpm typecheck/unit pass. |
| C — providers/routing/control-plane/execution | Extrair ports; migrar módulos por dependência topológica, não por árvore; evitar barrel que recria ciclos. | Grafo real satisfaz direção, sem pacote órfão/ciclo, testes por módulo e integração. |
| D — apps e ferramentas | Migrar edge/local-runtime, CLI, control-center; atualizar `.codex`, launchers, hooks, daemon e smoke tests na mesma onda compatível. | MCP inicia a partir do destino novo; daemon health ok; CLI/dashboard/accessibility smoke ok; nenhuma segunda instância. |
| E — docs/GitHub/workspace | PNPM workspace/lock/exports/CI, CodeQL/fuzz/security/Dependabot, CODEOWNERS/templates, docs e ADRs. | Frozen install; CI e security executam nos paths novos; required checks não ficam pending; owners válidos. |
| F — caminho e integração | Depois de encerrar uso do caminho antigo e todos consumidores migrarem, mudar diretório local; reabrir processos/ambiente; PR/CI; sincronizar GitHub. | Todos os gates do pedido original; zero referência operacional ativa indevida ao nome/path antigo. |

Não criar `apps/brain-api`, `apps/worker`, `packages/sdk`, plugins de provider sem código, nem infraestrutura cloud/window vazia. Esses alvos permanecem TODO até existir implementação com owner e aceite.

## 6. Como executar cada mudança

Para cada pacote: (1) adicionar/ajustar teste no destino ou teste de arquitetura; (2) observar falha relevante; (3) mover o menor conjunto coerente; (4) corrigir imports/config/exports; (5) rodar teste focado, typecheck, unit e checks de segurança afetados; (6) inspecionar diff, grafo, `git status` e compatibilidade; (7) atualizar o único Master Blueprint com evidência. Não usar movimentos em massa, não editar PNPM lockfile manualmente, não marcar DONE com diretório vazio ou só documentação.

## 7. Ondas aplicadas — snapshot histórico de 2026-09-25 (substituído pelo snapshot autoritativo acima)

- **Registry/config foundation:** seis arquivos movidos para `config/registries`; 15 fontes inexistentes foram substituídas por paths existentes ou `external://` explícito para dependências externas. Teste novo impede paths locais ausentes ou que escapem do repositório.
- **Shared contracts:** pacote `packages/contracts` contém validador JS, declaração pública, 12 schemas, tipos TS de Job e Runtime e compatibilidade por re-export dos caminhos anteriores. Workspace, testes JS/TS e typecheck estão integrados.
- **Brain context/memory:** compilador e memória persistente JSONL migrados para `packages/brain`; caminhos antigos são reexports compatíveis, dados sob `state/` não foram movidos. `memory-kernel` do Operating Core foi classificado como contrato tipado e `MemoryRetriever` como consumidor de dados in-memory, não outro store.
- **Evidence/history/cache:** FileHistoryStore e cache de contexto migrados para `packages/evidence`; wrapper MCG preserva o contrato. Store continua gravando no mesmo `MCG_ROOT/state`, sem cópia, limpeza ou transformação de dados.
- **Maestri Control Plane:** sessão/checkpoint/recovery, job engine, claim store, event-log adapter, receipt store e scheduler de turnos migrados para `packages/control-plane`; wrappers mantêm os caminhos antigos do Operating Core. O task store MCG em arquivos permanece separado até integração contratual e migração de dados comprovadas.
- **Architecture gate:** `tooling/scripts/check-package-architecture.mjs`, `test/architecture.test.mjs` e `pnpm check:architecture` detectam ciclos de pacotes, imports internos não declarados e imports relativos que atravessam pacote. O CI executa o gate; cenário atual passa em sete packages.
- **Edge/Local Runtime/Wire:** Local Runtime, MCP bridge, daemon local, Git adapter e testes vivem em `apps/edge`; o launcher executa Node diretamente sem `ComSpec`. O cliente Maestri Wire foi movido para `apps/edge/src/bridge/wire.mjs`; CLI e dashboard continuam funcionando pelo reexport legado `src/wire.mjs`. O daemon operacional MCG/Wire (processo Windows, inbox e estado) permanece ativo e não foi reiniciado/movido.
- **Daemon operacional:** reauditoria identificou o Wire client ativo e seu estado persistente separado; a tarefa agendada duplicada para o clone Desktop foi desabilitada, mas não se moveu estado nem se interrompeu a instância viva.
- **Verificação local da onda atual (2026-09-25):** `pnpm install --frozen-lockfile` passou; suíte raiz 41/41; Brain 2; Control Plane 7; Contracts 1 Node + 1 Vitest; Evidence 11; Context Gateway 6; Edge 34; Operating Core 65. Typechecks passaram em Brain, Contracts, Control Plane, Edge e Operating Core; arquitetura passou em 7 packages; syntax/import smoke passou em 35 módulos; scanner sensível passou em 183 arquivos; `git diff --check` não encontrou erro (somente avisos de normalização LF/CRLF do Windows). GitHub CI ainda não foi executado nesta branch. Estes são gates locais, não conclusão dos work packages.

## 8. Bloqueios registrados no snapshot de 2026-09-25 (histórico; ver estado atual no início)

- O rename do checkout local permanece bloqueado pelos consumers ativos registrados em `docs/blueprints/NB-01_SOURCE_OWNERSHIP_AND_LOCAL_PATH_AUDIT.md`; a migração de código no worktree pode prosseguir sem renomear o checkout físico.
- O checkout original `.lumenva` mantém consumidores ativos; esta implementação está em worktree Git isolado. Não copiar, misturar ou limpar alterações de outros worktrees.
- Não importar branches/código do CRM/voz, não modificar outros worktrees/sessões, não forçar push, não mesclar direto à `main` sem PR/checks.

**Falta para concluir MCG/Maestri nesta reestruturação:** migrar CLI (`bin/mcg.mjs`), dashboard e módulos de execução/recovery/provider/routing/governance ainda sob `src/` e `packages/operating-core`; completar ownership/arquitetura e testes de integração; validar CI/GitHub; redirecionar os MCP consumers ativos; executar cutover controlado de `MCG_ROOT` e autostart; e só depois renomear o checkout para o caminho canônico informado no blueprint. Nenhum commit/push/PR foi feito nesta etapa.
